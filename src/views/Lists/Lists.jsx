import React from 'react';
import { Outlet } from 'react-router';

const Lists = () => {
    return (
        <div>
            Lists
            <Outlet />
        </div>
    );
}

export default Lists;
