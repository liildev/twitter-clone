import { Outlet } from '@mui/icons-material';
import React from 'react';

const Messages = () => {
    return (
        <div>
            Messages
            <Outlet />
        </div>
    );
}

export default Messages;
